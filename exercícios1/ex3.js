/* Crie uma função que recebe dois parâmetros, base e 
expoente, e retorne a base elevada ao expoente */

let base = 0
let expoente = 0

exponenciacao = (base, expoente) => console.log(`O valor elevado é ${Math.pow(base, expoente)}`)

exponenciacao(base, expoente)